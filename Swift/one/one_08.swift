// Compute the area and perimeter of a circle with a radius of 5.5
// area = radius * radius * pi
// perimeter = 2 * pi * radius
let pi = 3.14159
var radius = 5.5
var area = radius * radius * pi
var perimeter = 2 * pi * radius
print("Area is \(area)")
print("Perimeter is \(perimeter)")
